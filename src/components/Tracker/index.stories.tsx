import { Meta, StoryObj } from "@storybook/react";

import { TrackerHero } from ".";

export default {
  title: "Components/Tracker",
  component: TrackerHero,
} as Meta;

export const Default: StoryObj = {};
