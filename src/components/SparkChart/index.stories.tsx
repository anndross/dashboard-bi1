import { Meta, StoryObj } from "@storybook/react";

import { SparkChartHero } from ".";

export default {
  title: "Components/SparkChart",
  component: SparkChartHero,
} as Meta;

export const Default: StoryObj = {};
