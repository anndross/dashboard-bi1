import { Meta, StoryObj } from "@storybook/react";

import { ProgressCircleHero } from ".";

export default {
  title: "Components/ProgressCircle",
  component: ProgressCircleHero,
} as Meta;

export const Default: StoryObj = {};
