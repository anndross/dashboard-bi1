import { Meta, StoryObj } from "@storybook/react";

import { LineChartHero } from ".";

export default {
  title: "Components/LineChart",
  component: LineChartHero,
} as Meta;

export const Default: StoryObj = {};
