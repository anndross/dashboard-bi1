import { Meta, StoryObj } from "@storybook/react";

import { BarList } from ".";

export default {
  title: "Components/BarList",
  component: BarList,
} as Meta;

export const Default: StoryObj = {};
